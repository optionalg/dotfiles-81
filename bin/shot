#!/bin/sh
#
# wildefyr - 2016 (c) CC
# wrapper script around xscreenshot and ff2png

SHOT=${SHOT:-./shot}

test $# -eq 0 && {
    xscreenshot | ff2png > "${SHOT}.png"
} || {
    wattr "$1" && xscreenshot "$1" | ff2png > "${SHOT}.png" || exit 1
}
