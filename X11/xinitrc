#!/bin/sh

xset s off
xset -dpms
xset +fp ~/.local/share/fonts
xsetroot -name background
xsetroot -cursor_name left_ptr

keys
urxvtd -q -o -f &

test -f ~/.Xresources && xrdb -load ~/.Xresources

test $(hostname) = "flame" && {
    synergys
} || {
    synergyc 2>&1 > /dev/null || synergyc flame
}

exec runfyre
